{
  "name": "demo-json-server",
  "version": "1.0.0",
  "description": "",
  "main": "mockDataSchema.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-mock-data": "node mock/buildScripts/generateMockData",
    "prestart-mockapi": "npm run generate-mock-data",
    "start-mockapi": "json-server --watch mock/api/db.json --port 3003"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-schema-faker": "^0.4.3",
    "json-server": "^0.12.0"
  }
}
